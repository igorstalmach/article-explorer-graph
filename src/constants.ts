export const SCREEN_WIDTH = window.innerWidth <= 600 ? 600 : window.innerWidth;
export const SCREEN_HEIGHT =
  window.innerHeight <= 600 ? 600 : window.innerHeight;
export const SCREEN_CENTER = [SCREEN_WIDTH / 2, SCREEN_HEIGHT / 2];
